{
  "name": "@oranje/mobile",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "App del Colaborador — Capacitor sobre el bundle de @oranje/web (solo /collaborator)",
  "scripts": {
    "build:web": "pnpm -F @oranje/web build:mobile",
    "sync": "pnpm run build:web && cap sync",
    "open:android": "cap open android",
    "open:ios": "cap open ios",
    "apk": "pnpm run sync && node scripts/gradle.mjs assembleDebug",
    "lint": "echo 'la app es el bundle de @oranje/web: se linta allá' && exit 0",
    "typecheck": "echo 'sin código TS propio fuera del capacitor.config' && exit 0",
    "test": "echo 'sin tests propios' && exit 0",
    "build": "pnpm run sync"
  },
  "dependencies": {
    "@capacitor/android": "^7.4.4",
    "@capacitor/core": "^7.4.4",
    "@capacitor/ios": "^7.4.4"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.4"
  }
}
