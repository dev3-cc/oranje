# `apps/mobile`

React Native. Colaborador e Inspector: ponche con GPS y foto, trabajo offline
(D-05, D-08).

Pendiente de armar. La app **captura**; el servidor decide si el ponche es
válido — nunca al revés (D-08).
